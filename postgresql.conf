logging_collector = on
log_directory = 'pg_log'
log_filename = 'postgresql-%Y-%m-%d_%H%M%S.log'
log_statement = 'all'  # Логировать все выполняемые запросы
log_min_duration_statement = 0  # Логировать все запросы, независимо от их длительности